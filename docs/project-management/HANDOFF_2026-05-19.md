# HANDOFF — 2026-05-19 (W2 Day 1)

> **작성자**: @team-lead
> **대상**: 다음 세션 / 팀원

---

## 현재 상태

### W1/W2 전체 Step 완료

| Step | 내용 | 상태 | 레포 |
|:----:|------|:----:|:----:|
| 1 | AWS 인프라 프로비저닝 | Done | — |
| 2 | Docker Compose 전체 로컬 환경 | Done | synapse-shared |
| 3 | CI/CD 파이프라인 (mirror + ci + deploy) | Done | synapse-shared + synapse-gateway |
| 4 | Kafka 토픽 설계/생성 | Done | synapse-shared |
| 5 | Schema Registry BACKWARD 호환성 | Done | synapse-shared |
| 6 | Gateway 라우팅 + Rate Limit + CORS | Done | synapse-gateway |

**W1 진행률**: 3/3 Steps 완료
**W2 진행률**: 5/5 Steps 완료 (이월분 포함)

### CI/CD 파이프라인 검증 완료

| 레포 | 워크플로 | 상태 |
|------|----------|:----:|
| synapse-shared | ci-java.yml (PR → build + Modulith verify) | PASS |
| synapse-shared | mirror.yml (push → synapse-mirror 동기화) | PASS |
| synapse-shared | schema-check.yml (*.avsc 변경 → 호환성 검증) | PASS |
| synapse-gateway | ci.yml (PR/push → Gradle build + test) | PASS |
| synapse-gateway | deploy.yml (push → ECR push + gitops tag) | PASS |

### 주요 커밋

**synapse-shared** (`main` → `4136883`):
- Docker Compose 13개 서비스 + .env.example + README
- deploy.yml 삭제 (라이브러리 레포, 배포 대상 아님)
- docker-compose.yml gateway stub → 실제 빌드 이미지 교체
- 문서 갱신 (TASK/WORKFLOW/HISTORY Step 1~6)

**synapse-gateway** (`master` → `afd41eb`):
- Spring Boot 4.0.6 + Spring Cloud 2025.1.1 + Gateway 5.0.1
- 프로그래밍 방식 라우트 4개 + RedisRateLimiter + CorsWebFilter
- Multi-stage Dockerfile + ci.yml + deploy.yml
- ECR 레포지토리 생성 완료

---

## 발견 사항 (다음 세션 참고)

1. **Spring Boot 4.0.6 YAML 라우트 버그**: `spring.cloud.gateway.routes` YAML 설정이 로드되지 않음 → `RouteLocatorBuilder`로 프로그래밍 방식 사용
2. **YAML globalcors 미동작**: `CorsWebFilter` 빈으로 대체
3. **Gradle 8.14+ 필수**: Boot 4.0.6이 8.14 이상 요구
4. **Dockerfile CRLF**: Windows 환경에서 `dos2unix` + `bash` 필요
5. **synapse-gateway 브랜치**: `master` 사용 중 (다른 레포는 `main`)

---

## 다음 작업 (W3: 05-26 ~ 05-29)

### Step 7: Kafka E2E 검증 + 코드 리뷰 조율
- platform → engagement 이벤트 흐름 테스트
- knowledge → learning 이벤트 흐름 테스트
- learning → engagement 이벤트 흐름 (card.reviewed → XP)
- engagement → platform 이벤트 흐름 (알림 트리거)
- 전체 서비스 코드 리뷰 1차

### Step 8: ArgoCD dev/staging 배포 검증
- dev 환경 4개 서비스 ArgoCD Sync
- staging 환경 수동 승인 배포 테스트
- Rollback 시나리오 테스트

### 사전 준비 필요
- 각 서비스 Kafka Producer/Consumer 구현 상태 확인 (팀원 진행 사항)
- synapse-gitops ArgoCD ApplicationSet 설정 확인
- 테스트 데이터 시드 준비

---

## 미해결 항목

| 항목 | 우선순위 | 비고 |
|------|:--------:|------|
| synapse-gateway `master` → `main` 변경 | Low | 다른 레포와 일관성, 급하지 않음 |
| deploy.yml Node.js 20 deprecation 경고 | Low | 2026-06 이후 actions/checkout@v5 등으로 업그레이드 |
| MSK 클러스터 토픽 반영 | Medium | 인프라 재생성 후 `scripts/create-kafka-topics.sh` 실행 |
