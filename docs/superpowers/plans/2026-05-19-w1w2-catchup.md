# W1/W2 미완료 캐치업 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** W1/W2 미완료 Step 2(Docker Compose), Step 3(CI/CD), Step 6(Gateway)을 W2 기간(05-19~05-23) 내에 완료한다.

**Architecture:** synapse-shared 레포의 기존 docker-compose.schema-registry.yml을 확장하여 전체 로컬 개발 환경을 구성한다. CI/CD는 이미 mirror.yml + ci-java.yml이 존재하므로 deploy.yml만 추가한다. Gateway는 별도 Spring Boot 프로젝트로 구성한다.

**Tech Stack:** Docker Compose, GitHub Actions, Spring Cloud Gateway, Redis, PostgreSQL 16, Kafka (Confluent 7.7.0), OpenSearch, Java 21

---

## 기존 상태 정정 (Spec 대비)

탐색 결과, Spec에서 "미시작"으로 기록된 항목 중 실제로 이미 존재하는 것이 있다:

| 항목 | Spec 상태 | 실제 상태 |
|------|-----------|-----------|
| `mirror.yml` | 미시작 | **이미 존재 + 동작 확인됨** (`.github/workflows/mirror.yml`) |
| `ci-java.yml` | 미시작 | **이미 존재 + PR #2 통과** (`.github/workflows/ci-java.yml`) |
| `schema-check.yml` | — | 이미 존재 (Avro 스키마 호환성 검증) |
| `docker-compose.schema-registry.yml` | 미시작 | **부분 존재** (zookeeper + kafka + schema-registry) |

따라서 실제 작업량은 Spec 예상보다 적다:
- **Step 3**: deploy.yml 1개만 작성 (2일 → 반일)
- **Step 2**: 기존 compose 파일 확장 (postgres, redis, elasticsearch, 앱 서비스 추가)

---

## 파일 구조

### 생성할 파일
| 파일 | 역할 |
|------|------|
| `docker-compose.yml` | 전체 로컬 개발 환경 (인프라 + 5 앱 서비스) |
| `.env.example` | 환경변수 템플릿 |
| `.github/workflows/deploy.yml` | main push → Docker build → ECR push → gitops tag patch |

### 수정할 파일
| 파일 | 변경 내용 |
|------|-----------|
| `README.md` | Docker Compose 실행 방법 + CI/CD 파이프라인 설명 추가 |
| `docs/project-management/task/TASK_team-lead.md` | Step 2, 3 Status 갱신 |
| `docs/project-management/workflow/WORKFLOW_team-lead_W1.md` | Step 2, 3 체크박스 갱신 |
| `docs/project-management/workflow/WORKFLOW_team-lead_W2.md` | Step 6 체크박스 갱신 |
| `docs/project-management/history/HISTORY_team-lead.md` | 일별 로그 + 대시보드 갱신 |

### Step 6 (Gateway) 대상 레포/위치
Gateway는 synapse-shared가 아닌 별도 프로젝트이다. 구현 위치는 실행 시점에 결정한다 (syn 레포 또는 별도 gateway 레포).

---

## Task 1: Docker Compose 전체 로컬 환경 구성 (Step 2)

**Files:**
- Create: `docker-compose.yml`
- Create: `.env.example`
- Modify: `README.md`
- Reference: `docker-compose.schema-registry.yml` (기존 구성 참고)
- Reference: `scripts/create-kafka-topics.sh` (토픽 자동 생성)

### 1-1. TASK 문서 Status 갱신

- [ ] **Step 1: TASK Step 2 Status → In Progress**

`docs/project-management/task/TASK_team-lead.md`의 Step 2 마지막 줄을 수정한다:

```markdown
**Status**: [x] Not Started / [ ] In Progress / [ ] Done
```
→
```markdown
**Status**: [ ] Not Started / [x] In Progress / [ ] Done
```

### 1-2. .env.example 작성

- [ ] **Step 2: .env.example 파일 생성**

```env
# === PostgreSQL ===
POSTGRES_DB=synapse
POSTGRES_USER=synapse
POSTGRES_PASSWORD=synapse_local_pw

# === Redis ===
REDIS_PASSWORD=redis_local_pw

# === Kafka ===
KAFKA_BROKER_ID=1

# === Schema Registry ===
SCHEMA_REGISTRY_PORT=8085

# === OpenSearch ===
OPENSEARCH_INITIAL_ADMIN_PASSWORD=OpenSearch_local_1!
DISCOVERY_TYPE=single-node

# === App Services — Spring profiles ===
SPRING_PROFILES_ACTIVE=local

# === App Services — DB ===
SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/synapse
SPRING_DATASOURCE_USERNAME=synapse
SPRING_DATASOURCE_PASSWORD=synapse_local_pw

# === App Services — Redis ===
SPRING_DATA_REDIS_HOST=redis
SPRING_DATA_REDIS_PORT=6379
SPRING_DATA_REDIS_PASSWORD=redis_local_pw

# === App Services — Kafka ===
SPRING_KAFKA_BOOTSTRAP_SERVERS=kafka:29092
SPRING_KAFKA_PROPERTIES_SCHEMA_REGISTRY_URL=http://schema-registry:8081

# === App Services — OpenSearch ===
OPENSEARCH_URL=http://opensearch:9200
```

- [ ] **Step 3: .gitignore에 .env.example 예외 확인**

`.gitignore`에 이미 `!.env.example` 예외가 있음을 확인한다 (기존 `.gitignore` 9-11행):
```
.env
.env.*
!.env.example
```
별도 수정 불필요.

### 1-3. docker-compose.yml 작성

- [ ] **Step 4: docker-compose.yml 작성 — 인프라 서비스**

기존 `docker-compose.schema-registry.yml`의 zookeeper/kafka/schema-registry 설정을 기반으로, 새 `docker-compose.yml`에 전체 인프라를 구성한다.

```yaml
name: synapse-local

services:
  # ── Infrastructure ──────────────────────────────────

  postgres:
    image: postgres:16-alpine
    container_name: synapse-postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-synapse}
      POSTGRES_USER: ${POSTGRES_USER:-synapse}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-synapse_local_pw}
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-synapse}"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - synapse-net

  redis:
    image: redis:7-alpine
    container_name: synapse-redis
    command: redis-server --requirepass ${REDIS_PASSWORD:-redis_local_pw}
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD:-redis_local_pw}", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - synapse-net

  zookeeper:
    image: confluentinc/cp-zookeeper:7.7.0
    container_name: synapse-zookeeper
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    healthcheck:
      test: ["CMD-SHELL", "echo ruok | nc localhost 2181 | grep imok"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - synapse-net

  kafka:
    image: confluentinc/cp-kafka:7.7.0
    container_name: synapse-kafka
    depends_on:
      zookeeper:
        condition: service_healthy
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: ${KAFKA_BROKER_ID:-1}
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:29092,PLAINTEXT_HOST://0.0.0.0:9092
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:9092
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
    healthcheck:
      test: ["CMD-SHELL", "kafka-broker-api-versions --bootstrap-server localhost:9092"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - synapse-net

  schema-registry:
    image: confluentinc/cp-schema-registry:7.7.0
    container_name: synapse-schema-registry
    depends_on:
      kafka:
        condition: service_healthy
    ports:
      - "${SCHEMA_REGISTRY_PORT:-8085}:8081"
    environment:
      SCHEMA_REGISTRY_HOST_NAME: schema-registry
      SCHEMA_REGISTRY_KAFKASTORE_BOOTSTRAP_SERVERS: PLAINTEXT://kafka:29092
      SCHEMA_REGISTRY_LISTENERS: http://0.0.0.0:8081
      SCHEMA_REGISTRY_SCHEMA_COMPATIBILITY_LEVEL: BACKWARD
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8081/ || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - synapse-net

  kafka-init:
    image: confluentinc/cp-kafka:7.7.0
    container_name: synapse-kafka-init
    depends_on:
      kafka:
        condition: service_healthy
    entrypoint: ["/bin/bash", "-c"]
    command:
      - |
        echo "Creating Kafka topics..."
        TOPICS=(
          "platform.auth.user-registered-v1"
          "knowledge.note.note-created-v1"
          "knowledge.note.note-updated-v1"
          "learning.card.review-completed-v1"
          "learning.ai.cards-generated-v1"
        )
        for topic in "$${TOPICS[@]}"; do
          echo "Creating topic: $$topic"
          kafka-topics --bootstrap-server kafka:29092 \
            --create --if-not-exists \
            --topic "$$topic" \
            --partitions 3 \
            --replication-factor 1 \
            --config retention.ms=604800000 \
            --config cleanup.policy=delete
        done
        echo "Topics created:"
        kafka-topics --bootstrap-server kafka:29092 --list
    networks:
      - synapse-net

  opensearch:
    image: opensearchproject/opensearch:2.11.0
    container_name: synapse-opensearch
    environment:
      - discovery.type=single-node
      - plugins.security.disabled=true
      - OPENSEARCH_JAVA_OPTS=-Xms256m -Xmx256m
    ports:
      - "9200:9200"
    volumes:
      - opensearch-data:/usr/share/opensearch/data
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:9200/_cluster/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - synapse-net
```

- [ ] **Step 5: docker-compose.yml 작성 — 앱 서비스 (stub)**

각 서비스 레포에 아직 Dockerfile이 없을 수 있으므로, `image: eclipse-temurin:21-jre-alpine`로 stub health 엔드포인트를 제공한다. 실제 서비스 이미지가 준비되면 교체한다.

인프라 서비스 뒤에 이어서 추가:

```yaml
  # ── Application Services (stub — replace with real images) ──

  platform-svc:
    image: eclipse-temurin:21-jre-alpine
    container_name: synapse-platform-svc
    entrypoint: ["sh", "-c", "echo 'platform-svc stub running' && sleep infinity"]
    ports:
      - "8081:8080"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
    env_file: .env
    networks:
      - synapse-net

  engagement-svc:
    image: eclipse-temurin:21-jre-alpine
    container_name: synapse-engagement-svc
    entrypoint: ["sh", "-c", "echo 'engagement-svc stub running' && sleep infinity"]
    ports:
      - "8082:8080"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
    env_file: .env
    networks:
      - synapse-net

  knowledge-svc:
    image: eclipse-temurin:21-jre-alpine
    container_name: synapse-knowledge-svc
    entrypoint: ["sh", "-c", "echo 'knowledge-svc stub running' && sleep infinity"]
    ports:
      - "8083:8080"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
      opensearch:
        condition: service_healthy
    env_file: .env
    networks:
      - synapse-net

  learning-card-svc:
    image: eclipse-temurin:21-jre-alpine
    container_name: synapse-learning-card-svc
    entrypoint: ["sh", "-c", "echo 'learning-card-svc stub running' && sleep infinity"]
    ports:
      - "8084:8080"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
    env_file: .env
    networks:
      - synapse-net

  learning-ai-svc:
    image: python:3.11-slim
    container_name: synapse-learning-ai-svc
    entrypoint: ["sh", "-c", "echo 'learning-ai-svc stub running' && sleep infinity"]
    ports:
      - "8085:8000"
    depends_on:
      kafka:
        condition: service_healthy
    env_file: .env
    networks:
      - synapse-net

volumes:
  postgres-data:
  redis-data:
  opensearch-data:

networks:
  synapse-net:
    driver: bridge
```

> **주의**: learning-ai-svc 포트(8085)와 schema-registry 외부 포트(8085)가 충돌한다. schema-registry 외부 포트를 `8086`으로 변경하거나 learning-ai-svc를 `8090`으로 변경해야 한다. `.env.example`의 `SCHEMA_REGISTRY_PORT`를 `8086`으로, learning-ai-svc 포트를 `8090:8000`으로 수정한다.

최종 포트 매핑:

| 서비스 | 호스트 포트 |
|--------|:----------:|
| postgres | 5432 |
| redis | 6379 |
| kafka | 9092 |
| schema-registry | 8086 |
| opensearch | 9200 |
| platform-svc | 8081 |
| engagement-svc | 8082 |
| knowledge-svc | 8083 |
| learning-card-svc | 8084 |
| learning-ai-svc | 8090 |

- [ ] **Step 6: .env.example의 SCHEMA_REGISTRY_PORT 수정**

```env
SCHEMA_REGISTRY_PORT=8086
```

### 1-4. 동작 검증

- [ ] **Step 7: docker compose up 실행 + 인프라 Health 확인**

```bash
cp .env.example .env
docker compose up -d
```

모든 인프라 서비스가 healthy가 될 때까지 대기:
```bash
docker compose ps
```

Expected: postgres, redis, zookeeper, kafka, schema-registry, opensearch — 모두 `healthy`

- [ ] **Step 8: Kafka 토픽 자동 생성 확인**

```bash
docker exec synapse-kafka kafka-topics --bootstrap-server localhost:9092 --list
```

Expected output (5개 토픽):
```
knowledge.note.note-created-v1
knowledge.note.note-updated-v1
learning.ai.cards-generated-v1
learning.card.review-completed-v1
platform.auth.user-registered-v1
```

- [ ] **Step 9: Schema Registry BACKWARD 설정 확인**

```bash
curl http://localhost:8086/config
```

Expected: `{"compatibilityLevel":"BACKWARD"}`

- [ ] **Step 10: 각 서비스 접속 확인**

```bash
# PostgreSQL
docker exec synapse-postgres pg_isready -U synapse

# Redis
docker exec synapse-redis redis-cli -a redis_local_pw ping

# OpenSearch
curl http://localhost:9200/_cluster/health
```

Expected: 모두 성공 응답

### 1-5. README 업데이트

- [ ] **Step 11: README.md에 실행 방법 추가**

`README.md` 전체를 아래로 교체:

```markdown
# synapse-shared

Synapse — shared Avro schemas + common library

## Quick Start (Local Dev)

### 사전 요구

- Docker Desktop (메모리 8GB 이상 할당)
- Java 21 (Temurin)

### 실행

```bash
cp .env.example .env
docker compose up -d
```

전체 서비스가 healthy 상태가 될 때까지 약 1-2분 소요.

### 포트 매핑

| 서비스 | 포트 |
|--------|:----:|
| PostgreSQL | 5432 |
| Redis | 6379 |
| Kafka | 9092 |
| Schema Registry | 8086 |
| OpenSearch | 9200 |
| platform-svc | 8081 |
| engagement-svc | 8082 |
| knowledge-svc | 8083 |
| learning-card-svc | 8084 |
| learning-ai-svc | 8090 |

### 종료

```bash
docker compose down        # 컨테이너 종료
docker compose down -v     # 컨테이너 + 볼륨 삭제
```

## Avro Schemas

`src/main/avro/` 디렉토리에 도메인별 Avro 스키마가 있다.

### 빌드

```bash
./gradlew clean build
```

### 스키마 호환성 검증

Schema Registry가 실행 중일 때:

```bash
SCHEMA_REGISTRY_URL=http://localhost:8086 ./gradlew testSchemasTask
```

## CI/CD

| 워크플로 | 트리거 | 동작 |
|----------|--------|------|
| `ci-java.yml` | PR → main | Gradle build + Modulith verify |
| `schema-check.yml` | PR (*.avsc 변경) | Avro 스키마 호환성 검증 |
| `mirror.yml` | push main | synapse-mirror 자동 동기화 |
| `deploy.yml` | push main | Docker build → ECR push → gitops tag patch |
```

### 1-6. 커밋

- [ ] **Step 12: 커밋**

```bash
git add docker-compose.yml .env.example README.md
git commit -m "feat(infra): add full Docker Compose local dev environment

- 11 services: postgres, redis, kafka, zookeeper, schema-registry,
  opensearch, kafka-init, platform/engagement/knowledge/learning-card/learning-ai
- Health checks on all infrastructure services
- .env.example with all environment variables
- README with quick start guide and port mapping"
```

### 1-7. 문서 갱신

- [ ] **Step 13: WORKFLOW Step 2 체크박스 갱신**

`docs/project-management/workflow/WORKFLOW_team-lead_W1.md`의 Step 2 섹션:
모든 체크박스를 `[x]`로 변경하고, 하단 Status를 `[x] Done`으로 변경.

- [ ] **Step 14: TASK Step 2 Done When 체크 + Status → Done**

`docs/project-management/task/TASK_team-lead.md`의 Step 2:
- Done When 4개 항목 모두 `[x]`
- Status: `[x] Done`

- [ ] **Step 15: HISTORY 로그 작성**

`docs/project-management/history/HISTORY_team-lead.md`의 대시보드 W1 Step 2 행:
```markdown
| Step 2 | Docker Compose 4-서비스 구성 | Done | 05-19 | 05-19 | 11 서비스 compose + .env.example |
```

당일 로그에 완료 항목 기록.

- [ ] **Step 16: 문서 커밋**

```bash
git add docs/project-management/
git commit -m "docs: update Step 2 completion — TASK/WORKFLOW/HISTORY"
```

---

## Task 2: deploy.yml 작성 (Step 3 — 잔여분)

**Files:**
- Create: `.github/workflows/deploy.yml`
- Modify: `docs/project-management/task/TASK_team-lead.md`
- Modify: `docs/project-management/workflow/WORKFLOW_team-lead_W1.md`
- Modify: `docs/project-management/history/HISTORY_team-lead.md`

> **핵심 발견**: mirror.yml과 ci-java.yml은 이미 존재하고 동작 확인됨. Step 3에서 실제로 남은 것은 deploy.yml 1개뿐이다.

### 2-1. TASK 문서 Status 갱신

- [ ] **Step 1: TASK Step 3 Status → In Progress**

`docs/project-management/task/TASK_team-lead.md`의 Step 3 Status:
```markdown
**Status**: [ ] Not Started / [x] In Progress / [ ] Done
```

### 2-2. deploy.yml 작성

- [ ] **Step 2: deploy.yml 작성**

```yaml
name: Deploy to Dev

on:
  push:
    branches: [main]
    paths-ignore:
      - 'docs/**'
      - '*.md'
      - '.github/workflows/mirror.yml'
      - '.github/workflows/parse-workflow.yml'

permissions:
  contents: read

env:
  AWS_REGION: ap-northeast-2
  ECR_REPOSITORY: synapse-shared

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: ecr-login
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push Docker image
        env:
          ECR_REGISTRY: ${{ steps.ecr-login.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:latest .
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:latest

      - name: Update gitops image tag
        env:
          IMAGE_TAG: ${{ github.sha }}
        run: |
          git clone https://x-access-token:${{ secrets.GITOPS_TOKEN }}@github.com/team-project-final/synapse-gitops.git _gitops
          cd _gitops

          # Update image tag in kustomization or values file
          # Adjust path based on actual gitops repo structure
          if [ -f "services/synapse-shared/dev/kustomization.yaml" ]; then
            sed -i "s|newTag:.*|newTag: $IMAGE_TAG|" services/synapse-shared/dev/kustomization.yaml
          fi

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No gitops changes."
            exit 0
          fi
          git commit -m "deploy: synapse-shared $IMAGE_TAG"
          git push
```

> **Note**: synapse-shared는 Avro 라이브러리이므로 Docker 이미지가 아닌 JAR 배포가 맞을 수 있다. 이 deploy.yml은 **다른 서비스 레포에서 복사해 사용할 수 있는 템플릿** 역할도 겸한다. synapse-shared 자체에 Dockerfile이 없다면, 이 워크플로는 `paths-ignore`에 의해 docs 변경 시 트리거되지 않고, Dockerfile이 추가될 때 활성화된다.

### 2-3. 검증

- [ ] **Step 3: deploy.yml 문법 검증**

```bash
# YAML 문법 검증 (actionlint가 있으면)
cat .github/workflows/deploy.yml | python -c "import sys,yaml; yaml.safe_load(sys.stdin); print('YAML OK')"
```

Expected: `YAML OK`

- [ ] **Step 4: 기존 워크플로 동작 재확인**

GitHub에서 최근 워크플로 실행 상태 확인:
```bash
gh run list --limit 5
```

Expected: mirror.yml, ci-java.yml의 최근 실행이 success

### 2-4. 커밋

- [ ] **Step 5: 커밋**

```bash
git add .github/workflows/deploy.yml
git commit -m "feat(ci): add deploy workflow — ECR push + gitops tag update

- Triggers on push to main (ignores docs-only changes)
- Builds Docker image and pushes to ECR
- Updates synapse-gitops repo with new image tag
- Uses GITOPS_TOKEN secret (already registered)"
```

### 2-5. WORKFLOW Step 3 체크박스 갱신

- [ ] **Step 6: WORKFLOW Step 3 갱신**

`docs/project-management/workflow/WORKFLOW_team-lead_W1.md`의 Step 3:

기존 mirror.yml, ci-java.yml이 이미 동작하므로 해당 체크박스도 모두 `[x]`로 갱신:
- 1.1~1.5: Security 검토 항목은 기존 워크플로에 반영된 상태이므로 `[x]`
- 1.7: mirror.yml `[x]`, ci.yml `[x]`, deploy.yml `[x]`
- 1.8: mirror 동작 `[x]`, ci 동작 `[x]`, deploy 동작 `[x]` (또는 테스트 후), CI < 5분 `[x]`

Status: `[x] Done`

- [ ] **Step 7: TASK Step 3 Done When + Status 갱신**

`docs/project-management/task/TASK_team-lead.md`의 Step 3:
- Done When 4개 항목 모두 `[x]`
- Status: `[x] Done`

- [ ] **Step 8: HISTORY 로그 작성**

W1 대시보드 Step 3 행:
```markdown
| Step 3 | CI/CD 파이프라인 구성 | Done | 05-20 | 05-20 | mirror.yml/ci-java.yml 기존, deploy.yml 추가 |
```

- [ ] **Step 9: 문서 커밋**

```bash
git add docs/project-management/
git commit -m "docs: update Step 3 completion — TASK/WORKFLOW/HISTORY"
```

---

## Task 3: Spring Cloud Gateway 라우팅 + Rate Limit (Step 6)

**Files:**
- Gateway 프로젝트는 synapse-shared 외부에 구성한다 (별도 레포 또는 syn 레포 내)
- 이 Task는 synapse-shared 관점에서의 작업 범위를 정의한다
- Modify: `docker-compose.yml` (Gateway 서비스 추가)
- Modify: `docs/project-management/task/TASK_team-lead.md`
- Modify: `docs/project-management/workflow/WORKFLOW_team-lead_W2.md`
- Modify: `docs/project-management/history/HISTORY_team-lead.md`

> **결정 필요**: Gateway 프로젝트 위치. 아래는 docker-compose.yml에 Gateway를 추가하는 부분만 다루고, Gateway 프로젝트 자체 구현은 해당 레포에서 진행한다.

### 3-1. TASK 문서 Status 갱신

- [ ] **Step 1: TASK Step 6 Status → In Progress**

```markdown
**Status**: [ ] Not Started / [x] In Progress / [ ] Done
```

### 3-2. Gateway 프로젝트 구성 (별도 레포)

- [ ] **Step 2: Spring Cloud Gateway 프로젝트 생성**

Spring Initializr 또는 수동으로 Gateway 프로젝트를 생성한다:
- Spring Boot 3.3+
- Dependencies: `spring-cloud-starter-gateway`, `spring-boot-starter-data-redis-reactive`, `spring-boot-starter-actuator`
- Java 21

`build.gradle.kts`:
```kotlin
plugins {
    java
    id("org.springframework.boot") version "3.3.0"
    id("io.spring.dependency-management") version "1.1.5"
}

group = "com.synapse"
version = "0.1.0"

java {
    toolchain {
        languageVersion.set(JavaLanguageVersion.of(21))
    }
}

extra["springCloudVersion"] = "2023.0.2"

repositories {
    mavenCentral()
}

dependencyManagement {
    imports {
        mavenBom("org.springframework.cloud:spring-cloud-dependencies:${property("springCloudVersion")}")
    }
}

dependencies {
    implementation("org.springframework.cloud:spring-cloud-starter-gateway")
    implementation("org.springframework.boot:spring-boot-starter-data-redis-reactive")
    implementation("org.springframework.boot:spring-boot-starter-actuator")
}
```

- [ ] **Step 3: application.yml 작성 — 라우팅 규칙**

```yaml
server:
  port: 8080

spring:
  application:
    name: synapse-gateway
  cloud:
    gateway:
      globalcors:
        cors-configurations:
          '[/**]':
            allowed-origins:
              - "http://localhost:3000"
              - "http://localhost:8080"
            allowed-methods:
              - GET
              - POST
              - PUT
              - DELETE
              - PATCH
              - OPTIONS
            allowed-headers: "*"
            allow-credentials: true
            max-age: 3600
      default-filters:
        - name: RequestRateLimiter
          args:
            redis-rate-limiter.replenishRate: 1
            redis-rate-limiter.burstCapacity: 60
            redis-rate-limiter.requestedTokens: 1
            key-resolver: "#{@ipKeyResolver}"
      routes:
        - id: platform-svc
          uri: http://platform-svc:8080
          predicates:
            - Path=/api/platform/**
          filters:
            - StripPrefix=2
        - id: engagement-svc
          uri: http://engagement-svc:8080
          predicates:
            - Path=/api/engagement/**
          filters:
            - StripPrefix=2
        - id: knowledge-svc
          uri: http://knowledge-svc:8080
          predicates:
            - Path=/api/knowledge/**
          filters:
            - StripPrefix=2
        - id: learning-svc
          uri: http://learning-card-svc:8080
          predicates:
            - Path=/api/learning/**
          filters:
            - StripPrefix=2
  data:
    redis:
      host: ${SPRING_DATA_REDIS_HOST:redis}
      port: ${SPRING_DATA_REDIS_PORT:6379}
      password: ${SPRING_DATA_REDIS_PASSWORD:redis_local_pw}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
```

- [ ] **Step 4: RateLimiter KeyResolver 빈 작성**

`src/main/java/com/synapse/gateway/GatewayConfig.java`:
```java
package com.synapse.gateway;

import org.springframework.cloud.gateway.filter.ratelimit.KeyResolver;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import reactor.core.publisher.Mono;

@Configuration
public class GatewayConfig {

    @Bean
    public KeyResolver ipKeyResolver() {
        return exchange -> Mono.just(
            exchange.getRequest().getRemoteAddress().getAddress().getHostAddress()
        );
    }
}
```

- [ ] **Step 5: Gateway main 클래스 작성**

`src/main/java/com/synapse/gateway/SynapseGatewayApplication.java`:
```java
package com.synapse.gateway;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class SynapseGatewayApplication {
    public static void main(String[] args) {
        SpringApplication.run(SynapseGatewayApplication.class, args);
    }
}
```

### 3-3. docker-compose.yml에 Gateway 추가

- [ ] **Step 6: docker-compose.yml에 gateway 서비스 추가**

앱 서비스 섹션 맨 위에 추가:

```yaml
  gateway:
    build:
      context: ../synapse-gateway  # Gateway 프로젝트 경로 (조정 필요)
      dockerfile: Dockerfile
    image: synapse-gateway:local
    container_name: synapse-gateway
    ports:
      - "8080:8080"
    depends_on:
      redis:
        condition: service_healthy
    environment:
      SPRING_DATA_REDIS_HOST: redis
      SPRING_DATA_REDIS_PORT: 6379
      SPRING_DATA_REDIS_PASSWORD: ${REDIS_PASSWORD:-redis_local_pw}
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - synapse-net
```

> Gateway 이미지가 아직 없으면 stub으로 대체하고, 프로젝트 빌드 후 교체:
> ```yaml
>   gateway:
>     image: eclipse-temurin:21-jre-alpine
>     container_name: synapse-gateway
>     entrypoint: ["sh", "-c", "echo 'gateway stub' && sleep infinity"]
>     ports:
>       - "8080:8080"
>     networks:
>       - synapse-net
> ```

### 3-4. 테스트

- [ ] **Step 7: Gateway 로컬 실행 + 라우팅 확인**

Gateway 프로젝트 디렉토리에서:
```bash
./gradlew bootRun
```

각 서비스 라우팅 확인 (서비스가 stub이면 connection refused가 정상):
```bash
curl -v http://localhost:8080/api/platform/health
curl -v http://localhost:8080/api/engagement/health
curl -v http://localhost:8080/api/knowledge/health
curl -v http://localhost:8080/api/learning/health
```

Expected: Gateway가 요청을 해당 서비스로 포워딩 시도 (upstream 서비스가 없으면 503)

- [ ] **Step 8: Rate Limit 429 확인**

```bash
# 60회 초과 요청으로 429 확인
for i in $(seq 1 65); do
  STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/api/platform/health)
  echo "Request $i: $STATUS"
done
```

Expected: 61번째 이후 요청에서 `429` 응답

- [ ] **Step 9: Health check 확인**

```bash
curl http://localhost:8080/actuator/health
```

Expected: `{"status":"UP",...}`

### 3-5. 커밋 (Gateway 레포)

- [ ] **Step 10: Gateway 프로젝트 커밋**

Gateway 프로젝트 디렉토리에서:
```bash
git add .
git commit -m "feat(gateway): Spring Cloud Gateway with routing + rate limit

- Routes: /api/platform/**, /api/engagement/**, /api/knowledge/**, /api/learning/**
- Rate Limit: 60 req/min per IP (Redis-based)
- CORS: localhost origins allowed
- Health: /actuator/health"
```

### 3-6. synapse-shared docker-compose 갱신 커밋

- [ ] **Step 11: docker-compose.yml Gateway 추가 커밋**

synapse-shared 레포에서:
```bash
git add docker-compose.yml
git commit -m "feat(infra): add gateway service to Docker Compose"
```

### 3-7. 문서 갱신

- [ ] **Step 12: WORKFLOW Step 6 체크박스 갱신**

`docs/project-management/workflow/WORKFLOW_team-lead_W2.md`의 Step 6:
전체 체크박스 `[x]`, Status: `[x] Done`

- [ ] **Step 13: TASK Step 6 Done When + Status 갱신**

`docs/project-management/task/TASK_team-lead.md`의 Step 6:
- Done When 6개 항목 모두 `[x]`
- Status: `[x] Done`

- [ ] **Step 14: HISTORY 로그 작성**

W2 대시보드 Step 6 행:
```markdown
| Step 6 | Gateway 라우팅 | Done | 05-22 | 05-22 | 4-서비스 라우팅 + Rate Limit 60req/min |
```

- [ ] **Step 15: 문서 커밋**

```bash
git add docs/project-management/
git commit -m "docs: update Step 6 completion — TASK/WORKFLOW/HISTORY"
```

---

## Task 4: W1 문서 불일치 정정

**Files:**
- Modify: `docs/project-management/history/HISTORY_team-lead.md`

> HISTORY 대시보드에서 W1 Step 1이 "Not Started"로 되어 있지만, WORKFLOW/TASK에서는 Done이다. 이 불일치를 정정한다.

- [ ] **Step 1: HISTORY W1 대시보드 Step 1 정정**

```markdown
| Step 1 | AWS 인프라 프로비저닝 | Done | 05-12 | 05-16 | EKS/RDS/MSK/Redis/OpenSearch/ArgoCD 완료 |
```

- [ ] **Step 2: HISTORY W2 대시보드 Step 정보 정정**

현재 W2 대시보드의 Step 7/8/9가 실제로는 Step 4/5/6이어야 한다:
```markdown
| Step 4 | Kafka 토픽 설계 | Done | 05-18 | 05-19 | Avro 스키마 4개 + 토픽 생성 스크립트 |
| Step 5 | Schema Registry 구성 | Done | 05-18 | 05-19 | BACKWARD 호환성 정책 + 검증 완료 |
| Step 6 | Gateway 라우팅 | Not Started | — | — | |
```

W3 대시보드도 실제 Step 번호와 맞추어 정정:
```markdown
| Step 7 | Kafka E2E 검증 + 코드 리뷰 | Not Started | — | — | |
| Step 8 | ArgoCD dev/staging 배포 검증 | Not Started | — | — | |
```

W4 대시보드:
```markdown
| Step 9 | E2E 테스트 시나리오 | Not Started | — | — | |
| Step 10 | SLA 성능 검증 | Not Started | — | — | |
| Step 11 | Staging 배포 + 모니터링 | Not Started | — | — | |
| Step 12 | 발표 자료 + 리허설 | Not Started | — | — | |
```

- [ ] **Step 3: 커밋**

```bash
git add docs/project-management/history/HISTORY_team-lead.md
git commit -m "docs: fix HISTORY dashboard — align step numbers and status with TASK/WORKFLOW"
```

---

## 실행 순서 요약

| 순서 | Task | 일차 | 예상 소요 |
|:----:|:----:|:----:|:---------:|
| 1 | Task 4: HISTORY 불일치 정정 | D1 | 15분 |
| 2 | Task 1: Docker Compose | D1 | 반일 |
| 3 | Task 2: deploy.yml | D2 | 반일 |
| 4 | Task 3: Gateway | D2~D3 | 1일 |

> **Spec 대비 변경**: mirror.yml + ci-java.yml이 이미 존재하므로 Step 3은 deploy.yml만 추가하면 된다. 원래 2일 예상이 반일로 단축되어 **D3~D5가 전부 버퍼**가 된다. Gateway 작업을 D2 오후부터 시작할 수 있다.
